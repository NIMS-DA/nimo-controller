We will run an experiment that samples the Ackley function by random exploration. Perform exactly 20 experimental cycles. In every cycle, choose the next candidate using the selection tool with the RE (random exploration) method, then evaluate the ackley_function tool with the proposed values of x1 and x2 and register the returned value as the objective via an update. After all 20 cycles are finished, plot the distribution.