We will run a color-matching experiment on the mixing robot, minimizing the color difference to the target color 00ffff. First, move the robot to the home position once. Then perform exactly 12 mixing cycles. In each of the first 4 cycles, choose the next candidate using the selection tool with the RE (random exploration) method. In each of the remaining 8 cycles, choose the next candidate using the minimization tool with the PHYSBO method. In every cycle, handle the three colors one at a time, in the order red, blue, yellow. For each color: move to that color's well, aspirate the proposed volume of that color (proposal.red, proposal.blue or proposal.yellow), move to the mixing well whose number equals the current cycle counter (starting from 0), and dispense the same proposed volume there. After all three colors have been dispensed, evaluate get_color_diff on the current cycle's mixing well against the target hex color 00ffff and register the returned value as the objective via an update. After all 12 cycles are finished, plot the history of the best objective value in minimization mode.